$ vagrant init

change config.vm.box = "ubuntu/xenial64" from Vagrantfile

$ vagrant up

- on the terminal

$ platziverse-deploy
$ mkdir ssh
$ cd ssh
$ ssh-keygen -t rsa -C "oscarzambrano116@gmail.com"

- After installation

$ vagrant ssh

inside the ubuntu server

$ sudo su
$ cd .ssh
$ vim authorized_keys
-> paste your ssh public keygen
:wq 
exit

platziverse-deploy $ ssh root@127.0.0.1 -p 2222 i ssh/deploy

default: SSH address: 127.0.0.1:2202
default: SSH username: vagrant
default: SSH auth method: private key

$ ansible-playbook -i inventory.ini backend.yml --private-key ssh/deploy